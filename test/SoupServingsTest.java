import org.junit.Test;

public class SoupServingsTest {
	@Test
	public void test() {
//		SoupServings s = new SoupServings();
//		double result;
//		result = s.soupServings(50);
//		Assert.assertThat(result, Matchers.closeTo(0.625, 0.01));
//
//		result = s.soupServings(100);
//		Assert.assertThat(result, Matchers.closeTo(0.71875, 0.01));
//
//		result = s.soupServings(120);
//		Assert.assertThat(result, Matchers.closeTo(0.74219, 0.01));
//
//		result = s.soupServings(125);
//		Assert.assertThat(result, Matchers.closeTo(0.74219, 0.01));
	}
}